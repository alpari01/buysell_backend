stages:
 - build
 - deploy

build backend:
 stage: build
 only:
  refs:
   - master
 tags:
  - main
 script:
  - ./mvnw package
  - docker build -t web-project .

deploy backend:
 stage: deploy
 only:
  refs:
   - master
 tags:
  - main
 script:
  - cd /home/ubuntu/web-project
  - docker-compose down
  - docker-compose up -d
